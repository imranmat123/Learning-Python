class privledge:
	def __init__(self):
		self.privledge = ['can get user', 'can log in accounts',
						  'can log people'
						  'out']

	def check_your_privaledge(self):
		return print(self.privledge)